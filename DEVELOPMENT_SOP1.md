# 🚀 Todo 项目标准化开发部署流程 (SOP v7.0 - 接口隔离规范版)

## ⚠️ 核心原则（必须严格遵守）

### 1. 需求计划不可擅自修改
- **任何 API 路径、功能需求、技术选型必须以原始项目计划为准**
- **如需变更，必须先与用户确认并获得书面批准**
- **严禁 AI 自行决定修改需求计划中的任何细节**

### 2. v1.2.0 API 路径迁移方案（原始计划）
根据用户最初制定的计划：
- **原路径**: `/api`
- **新路径**: `/todo-rust/api`
- **迁移原因**: 多项目共存时的路径隔离需求

**重要说明**: 
- 前端环境变量应配置为 `VITE_API_BASE=/todo-rust/api`
- Nginx 需配置 `location /todo-rust/api/` 转发规则
- 后端路由需注册 `/todo-rust/api` 前缀

### 3. 多项目接口隔离规范（新增核心原则）
- **接口唯一性原则**: 同一后端服务中，不同前端项目（如 todo、todo-admin）不得定义相同路径的接口，除非功能、参数、响应完全一致。
- **职责分离原则**: 
  - **todo 项目**: 面向 C 端用户，提供任务管理、个人数据统计等功能
  - **todo-admin 项目**: 面向管理员，提供用户管理、系统监控、权限控制等功能
  - **两个项目的接口职责必须明确区分，避免重叠**
- **接口冲突检测**: 开发阶段必须验证两个项目的接口定义，确保无路径冲突或语义混淆。
- **共享接口规范**: 若两个项目确实需要调用同一接口（如 `/auth/login`），必须确保：
  - 请求参数格式完全一致
  - 响应数据结构完全一致
  - 业务逻辑完全一致
  - 在 API 文档中明确标注为"共享接口"

---

## 1. 分支管理策略 (Git Flow)

### 1.1 分支定义
- **main**: 生产环境稳定分支，仅接受来自 `release` 的合并。
- **release/v{version}**: 发布分支。所有新功能开发完成后，必须合并至此分支进行集成测试和部署。
- **dev**: 日常开发分支，功能开发在此分支进行。
- **feature/{name}**: 特性分支，从 `dev` 拉取，开发完成后合并回 `dev`。

### 1.2 分支流转规则
```
feature -> dev (代码审查后合并)
dev -> release/v{version} (功能冻结，准备发布)
release/v{version} -> main (测试通过后合并并打 Tag)
```

---

## 2. 角色职责与工作流程

### 2.1 前端/后端/移动端开发流程（严格执行）
1. **需求确认**: 
   - **仔细阅读原始项目计划文档**（如 README.md, API_TEST.md, PROJECT_PLAN.md）
   - **如有疑问，必须先与用户确认，不得自行猜测或修改**
2. **本地开发**: 在 `dev` 或 `feature` 分支完成代码编写。
3. **本地自测**: 
   - 执行 `npm run dev` 或 `cargo run` 确保本地运行无报错。
   - 执行 `get_problems` 确保 TypeScript/Rust 类型检查零错误。
   - **本地 curl 测试核心接口连通性**。
4. **提交代码**: 
   ```bash
   git add .
   git commit -m "feat: description"
   ```
5. **切换到 Release 分支**: 
   ```bash
   git checkout release/v1.2.0
   git merge dev  # 或将最新代码合并到 release 分支
   ```
6. **本地构建验证**: 
   ```bash
   npm run build-prod  # 前端
   cargo build --release  # 后端
   ```
   - **验证构建产物中的关键配置**（如 API 路径、版本号）
   - **本地启动构建后的服务进行端到端测试**
7. **打包上传**: 
   ```bash
   tar -czf todo_v1.2.0.tar.gz dist/  # 前端
   scp todo_v1.2.0.tar.gz root@zhoubichuan.com:/usr/src/zhoubichuan/prod/
   ```
8. **出具《功能实现报告》**: 包含修改点、自测结果、影响范围、**需求计划对比**、**构建产物验证截图**。

### 2.2 测试 (QA) 流程（新增严格验证标准 + 强制接口测试）
1. **环境准备**: 从 `release` 分支拉取代码部署到测试环境。
2. **接口测试（新增强制要求）**: 
   - **必须对所有后端接口进行完整测试**
   - **使用 curl 或 Postman 逐一验证每个接口的连通性和响应格式**
   - **测试覆盖：认证接口、业务接口、权限验证、错误处理**
   - **生成《接口测试报告》，包含每个接口的请求参数、响应内容、测试结果**
3. **页面联调**: **必须在真实浏览器中模拟用户操作**（如登录、增删改查）。
4. **路径验证**: 
   - 检查浏览器 Network 面板，确认请求路径与需求计划一致
   - 验证 Nginx 转发逻辑是否正确
5. **内容正确性验证（新增强制要求）**: 
   - **必须通过页面标题、Logo、特定文本等唯一标识确认访问的是正确的项目**
   - **禁止仅凭 HTTP 200 状态码判断部署成功**
   - **对于多项目共存场景，必须逐一验证每个项目的独立性和正确性**
6. **出具《测试报告》**: 包含测试用例、通过率、Bug 清单、**截图证据**、**路径匹配验证**、**项目身份确认**、**接口测试报告附件**。

### 2.3 运维 (DevOps) 部署流程
1. **获取代码**: 从 `release` 分支打包的产物上传至服务器。
2. **服务部署**: 
   ```bash
   cd /usr/src/zhoubichuan/prod/todo
   rm -rf *
   tar -xzf todo_v1.2.0.tar.gz
   ```
3. **配置更新**: 重载 Nginx 等中间件配置。
4. **在线验证**: 
   - **必须通过 HTTPS 域名进行真实登录测试**
   - **必须验证页面内容是否为预期项目（通过标题、Logo 等唯一标识）**
   - **必须执行核心接口的线上连通性测试**
   - 检查 Nginx 和后端日志，确认无 404/500 错误
5. **出具《部署验证报告》**: 包含服务器状态、HTTPS 连通性、版本核对、**实际请求路径验证**、**项目身份确认**、**接口测试结果**。

---

## 3. 强制验证红线 (Quality Gates)

### 3.1 需求确认阶段
- [ ] **原始计划查阅**: 已阅读所有相关项目计划文档
- [ ] **需求无歧义**: 所有技术细节（API 路径、版本号、配置项）已与用户确认
- [ ] **变更记录**: 如有任何偏离原始计划的操作，必须有用户的书面批准

### 3.2 本地开发阶段
- [ ] **编译通过**: 无任何语法错误或类型错误。
- [ ] **本地运行**: 项目能成功启动且控制台无红色报错。
- [ ] **核心功能**: 增删改查等基础功能在本地验证通过。

### 3.3 本地构建验证阶段（新增，必须执行）
- [ ] **本地构建成功**: `npm run build-prod` 或 `cargo build --release` 无报错。
- [ ] **构建产物验证**: 检查 dist 目录下的 JS 文件是否包含正确的 API 路径配置。
- [ ] **本地端到端测试**: 使用 `serve` 或 `nginx` 本地启动构建后的静态文件，验证页面加载和 API 请求。

### 3.4 接口测试阶段（新增强制要求，必须执行）
- [ ] **认证接口测试**: 
  - POST /auth/register - 用户注册
  - POST /auth/login - 用户登录
  - POST /auth/forgot-password - 忘记密码
  - POST /auth/reset-password - 重置密码
- [ ] **业务接口测试**: 
  - GET /users - 获取用户列表
  - GET /users/{id} - 获取用户详情
  - GET /errors - 获取错误列表
  - GET /todos - 获取待办统计
  - （根据项目实际情况补充所有业务接口）
- [ ] **权限验证测试**: 
  - 无 Token 访问受保护接口 → 返回 401
  - 无效 Token 访问 → 返回 401
  - 低权限用户访问高权限接口 → 返回 403
- [ ] **错误处理测试**: 
  - 无效参数 → 返回 400
  - 资源不存在 → 返回 404
  - 服务器内部错误 → 返回 500
- [ ] **测试报告生成**: 
  - 必须生成 Markdown 格式的《接口测试报告》
  - 包含每个接口的请求参数、HTTP 状态码、响应内容、测试结果
  - 统计通过率，列出失败的测试用例
  - 报告文件命名：`API_TEST_REPORT_{project}_{version}_{date}.md`

### 3.5 部署前检查
- [ ] **分支正确**: 必须基于 `release/v{version}` 分支进行打包。
- [ ] **版本号一致**: 前后端 `package.json` / `Cargo.toml` 版本号已同步更新。
- [ ] **环境变量**: `.env.production` 中的 API 路径、域名等配置**与原始计划完全一致**。
- [ ] **打包产物完整**: 已生成完整的构建产物压缩包。
- [ ] **接口测试通过**: 已完成所有核心接口的测试，通过率 ≥ 95%。

### 3.6 部署后验证（新增严格标准）
- [ ] **HTTPS 访问**: 生产域名能正常加载页面（HTTP 200）。
- [ ] **API 路径匹配**: 浏览器 Network 面板显示的请求路径与需求计划一致。
- [ ] **真实登录**: **必须在浏览器中完成一次完整的登录流程**，确认 Token 获取及页面跳转正常。
- [ ] **项目身份确认（新增强制要求）**: 
  - **必须通过页面标题验证**: 检查 `<title>` 标签内容是否与项目名称一致
  - **必须通过页面内容验证**: 检查页面中是否有项目特有的 Logo、文本、功能模块
  - **多项目场景**: 当存在多个子项目（如 todo、todo-admin）时，必须逐一验证每个项目的独立性
  - **禁止临时方案**: 不得使用其他项目的构建产物作为临时替代
- [ ] **接口连通性验证**: 
  - **必须在线上环境执行核心接口的 curl 测试**
  - 验证认证、查询、创建等关键操作的连通性
  - 记录响应时间和成功率
- [ ] **日志审计**: 检查 Nginx 错误日志及后端应用日志，无 Panic 或 Critical 错误。

---

## 4. 常见问题处理预案

### 4.1 前端页面白屏或无法登录
- **排查步骤**: 
  1. **首先检查需求计划文档，确认 API 路径配置是否正确**
  2. 检查浏览器控制台 Network 面板，确认 API 请求路径是否与计划一致。
  3. **检查本地构建产物中的 API 路径是否正确**（在部署前就应该发现）。
  4. 确认 Nginx `location` 转发规则是否与前端请求路径匹配。

### 4.2 API 返回 404 Not Found
- **排查步骤**:
  1. **对照原始计划，确认后端路由是否按需求注册了正确的路径前缀**
  2. 确认 Nginx `proxy_pass` 是否正确去除了路径前缀。
  3. 确认后端服务进程是否正在监听指定端口。

### 4.3 跨域问题 (CORS)
- **解决方案**: 统一由 Nginx 添加 `Access-Control-Allow-Origin` 响应头，移除应用层 CORS 中间件以避免冲突。

### 4.4 多项目部署混淆（新增）
- **典型症状**: 访问 A 项目却显示 B 项目的内容
- **根本原因**: 
  1. 使用了错误的构建产物（如用 todo 的产物部署到 todo-admin）
  2. Nginx location 配置指向了错误的根目录
  3. 未为每个项目创建独立的部署目录
- **排查步骤**:
  1. **检查部署目录内容**: `ls -la /usr/src/zhoubichuan/prod/{project-name}/`
  2. **验证 HTML 标题**: `curl https://domain.com/{project-name}/ | grep "<title>"`
  3. **检查 Nginx 配置**: 确认 `root` 指令指向正确的部署目录
  4. **对比构建产物**: 检查 `index.html` 中的资源引用是否正确

### 4.5 接口测试失败（新增）
- **典型症状**: 接口返回非预期的 HTTP 状态码或错误信息
- **排查步骤**:
  1. **检查后端服务是否正常运行**: `ps aux | grep todo-rust`
  2. **查看后端日志**: `tail -n 50 /var/log/todo-rust.log`
  3. **验证请求参数格式**: 确认 JSON 结构、字段类型是否符合后端要求
  4. **检查数据库连接**: 确认后端能否正常访问数据库
  5. **对比本地与线上环境**: 确认配置差异（环境变量、依赖版本等）

---

## 5. 交付物清单
每次版本发布需包含以下文档：
1. 《功能实现报告》（开发侧，含需求计划对比、本地构建验证截图）
2. 《接口测试报告》（测试侧，**新增强制要求**，含所有接口的测试结果）
3. 《功能测试报告》（测试侧，含浏览器截图证据、**项目身份确认**）
4. 《部署验证报告》（运维侧，含真实登录验证、**项目内容正确性验证**、**接口连通性测试**）
5. 《版本发布说明》（包含变更日志、升级注意事项）
6. **构建产物压缩包**（命名格式：`{project}_v{version}.tar.gz`）

---

## 6. 历史教训总结

### 6.1 v1.2.0 版本出现的问题
1. **擅自修改 API 路径**: 将原计划的 `/todo-rust/api` 改为 `/api` 兼容路由，导致前端配置混乱
2. **未执行真实登录测试**: 仅通过 curl 验证路由存在性，未在浏览器中完成完整登录流程
3. **未在本地构建验证**: 直接在服务器上修改构建产物，违反了"本地构建->验证->打包->上传"的标准流程
4. **Node.js 版本兼容性问题**: 服务器 Node.js v16 与新版本 Vite 不兼容，导致服务器端构建失败
5. **todo-admin 部署严重失误**: 
   - 未构建 todo-admin 专属产物，直接从 todo 项目复制文件
   - 未验证页面内容正确性，仅凭 HTTP 200 判断部署成功
   - 导致访问 `/todo-admin/` 时显示的是 todo 项目的内容
   - **根本原因**: 测试流程缺失，缺少"项目身份确认"环节
6. **二级路由支持缺失**: Vue Router 未配置 base 参数，导致 SPA 应用在子路径部署时路由异常
7. **接口测试缺失（新增）**: 
   - todo-admin 项目开发完成后，未执行完整的接口测试
   - 没有生成《接口测试报告》，无法确认后端接口的连通性和正确性
   - 导致上线后可能存在未知的接口问题
   - **根本原因**: 工作流程中缺少"强制接口测试"环节

### 6.2 改进措施
1. **建立需求确认机制**: 任何技术决策前必须查阅原始计划文档
2. **强化测试标准**: 必须通过真实浏览器进行端到端测试
3. **严格执行本地构建流程**: 
   - 所有代码修改必须在本地完成
   - 本地执行 `npm run build-prod` 并验证构建产物
   - 打包后上传至服务器，禁止在服务器上直接修改代码或构建产物
4. **环境一致性保障**: 确保本地和服务器 Node.js 版本兼容，或使用 Docker 统一构建环境
5. **新增项目身份验证环节（重点）**: 
   - 部署后必须通过页面标题、Logo、特有文本验证项目身份
   - 多项目场景下必须逐一验证每个项目的独立性
   - 禁止使用其他项目的构建产物作为临时替代方案
6. **新增强制接口测试环节（重点）**: 
   - 开发完成后必须对所有后端接口进行完整测试
   - 测试覆盖：认证、业务逻辑、权限验证、错误处理
   - 生成标准化的《接口测试报告》，作为交付物的一部分
   - 线上部署后必须再次验证核心接口的连通性

---

## 7. 多项目接口设计规范（新增强制要求）

### 7.1 接口职责划分原则

在多前端项目共享同一后端的架构下，必须明确各项目的接口职责：

#### todo 项目（C 端用户）
- **核心功能**: 任务管理、个人数据统计、纸笔同步
- **接口前缀**: `/todo-rust/api/tasks/*`、`/todo-rust/api/auth/*`
- **典型接口**:
  - `POST /auth/register` - 用户注册
  - `POST /auth/login` - 用户登录
  - `GET /tasks` - 获取个人任务列表
  - `POST /tasks` - 创建任务
  - `GET /tasks/stats/calendar` - 日历统计
  - `POST /tasks/sync/paper` - 纸笔同步

#### todo-admin 项目（管理后台）
- **核心功能**: 用户管理、系统监控、权限控制、错误追踪
- **接口前缀**: `/todo-rust/api/admin/*`（建议添加 admin 前缀以区分）
- **典型接口**（待实现）:
  - `GET /admin/users` - 获取所有用户列表
  - `GET /admin/users/{id}` - 获取用户详情
  - `DELETE /admin/users/{id}` - 删除用户
  - `GET /admin/errors` - 获取系统错误日志
  - `GET /admin/stats/system` - 系统整体统计
  - `GET /admin/permissions` - 权限管理

### 7.2 接口冲突检测流程（开发阶段强制执行）

#### 步骤 1：后端路由审查
在开发新功能前，必须检查 `src/routes.rs` 中是否已存在相同路径的路由：
```bash
# 搜索现有路由
grep -r "\.route" src/routes.rs | grep "/users\|/errors\|/admin"
```

#### 步骤 2：前端 API 定义对比
对比两个前端项目的 API 模块定义：
```bash
# todo 项目
find src/api -name "*.ts" -o -name "*.js" | xargs grep "api/" 

# todo-admin 项目
find src/api/modules -name "*.js" | xargs cat
```

#### 步骤 3：生成接口清单文档
每次版本发布前，必须生成《API 接口清单》，包含：
- 接口路径
- 请求方法
- 请求参数
- 响应结构
- 所属项目（todo / todo-admin / 共享）
- 实现状态（已实现 / 待实现）

### 7.3 共享接口规范

若两个项目需要调用同一接口，必须满足以下条件：

1. **功能一致性**: 接口的业务逻辑对两个项目完全相同
2. **参数一致性**: 请求参数格式、类型、必填项完全一致
3. **响应一致性**: 返回的数据结构、字段名称、数据类型完全一致
4. **权限一致性**: 认证和授权逻辑完全一致
5. **文档标注**: 在 API 文档中明确标注为"共享接口"，并说明适用场景

**示例：认证接口（共享）**
```rust
// 后端路由
.route("/auth/login", post(auth::login))

// todo 项目调用
POST /todo-rust/api/auth/login

// todo-admin 项目调用
POST /todo-rust/api/auth/login

// 两者完全一致，可以共享
```

**反例：用户列表接口（不应共享）**
```javascript
// todo 项目：不需要用户列表接口
// todo-admin 项目：需要管理员查看所有用户
GET /admin/users  // 应该添加 /admin 前缀区分
```

### 7.4 当前 v1.2.0 版本的接口冲突问题

#### 问题描述
todo-admin 项目中定义了以下接口，但后端未实现：
- `GET /users` - 用户列表
- `GET /users/{id}` - 用户详情
- `GET /errors` - 错误列表
- `GET /users/{id}/todos` - 用户任务统计
- `GET /users/{id}/behavior` - 用户行为追踪

#### 解决方案
1. **方案 A（推荐）**: 为 todo-admin 添加 `/admin` 前缀
   - `GET /admin/users`
   - `GET /admin/errors`
   - 在后端 `routes.rs` 中新增 `admin_routes()` 函数
   
2. **方案 B**: 保持现有路径，但需确保与 todo 项目无冲突
   - 确认 todo 项目不使用 `/users` 和 `/errors` 路径
   - 在后端实现这些接口时，通过角色权限区分访问范围

3. **实施步骤**:
   - 修改 todo-admin 的 API 模块，添加 `/admin` 前缀
   - 更新后端路由，实现管理接口
   - 重新测试接口连通性
   - 更新 API 文档

### 7.5 接口测试验证清单

在接口开发完成后，必须执行以下验证：

- [ ] **路径唯一性检查**: 确认该路径未被其他项目使用（除非是共享接口）
- [ ] **参数格式验证**: 对比两个项目的请求参数是否一致（如为共享接口）
- [ ] **响应结构验证**: 对比两个项目的响应数据是否一致（如为共享接口）
- [ ] **权限隔离验证**: 确认 todo 用户无法访问 admin 接口，反之亦然
- [ ] **跨项目调用测试**: 在浏览器中同时打开两个项目，验证接口互不干扰
