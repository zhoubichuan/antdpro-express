# 🚀 全栈自动化协作与部署流程规范 (SOP v6.2 - 版本化管理版)

## ⚠️ 核心原则（必须严格遵守）

1.  **版本号先行**: **每次需求开发前必须先确认并更新项目版本号**。所有分支、报告、交付物必须携带该版本号。
2.  **需求计划不可擅自修改**: 任何 API 路径、功能需求必须以原始计划为准。
3.  **API 路径规范**: 统一使用 `/api/{resource}` 前缀，生产环境通过 Nginx 代理。
4.  **空闲即执行 (Idle = Execute)**: 任务完成后立即领取下一项，严禁等待指令。
5.  **Git 兜底与激进开发**: 允许直接修改代码文件，错误通过 `git revert` 修复。
6.  **人类时间汇报**: 每 30 分钟输出进度报告，不阻塞任务流。

---

## 1. 版本管理与分支策略 (Version & Git Flow)

### 1.1 版本号规范
- **格式**: `v{Major}.{Minor}.{Patch}` (例如: `v1.2.0`)
- **确认时机**: 
  - **需求启动前**: PM 必须根据需求复杂度确定新版本号，并更新根目录 `package.json` 中的 `version` 字段。
  - **分支命名**: 所有特性分支和发布分支必须包含版本号。

### 1.2 分支定义与命名
- **master**: 生产环境稳定分支。
- **release/v{version}**: 发布分支 (例如: `release/v1.2.0`)。用于集成测试和预发布。
- **dev**: 日常开发分支。
- **feature/{version}/{name}**: 特性分支 (例如: `feature/v1.2.0/user-auth`)。从 `dev` 拉取，合并回 `dev`。

### 1.3 分支流转规则
```
feature/v1.2.0/xxx -> dev (代码审查后合并)
dev -> release/v1.2.0 (功能冻结，准备发布)
release/v1.2.0 -> master (测试通过后合并并打 Tag: v1.2.0)
```

---

## 2. 角色自动执行清单 (Auto-Execution List) - **核心驱动**

### 👷‍♂️ DevOps Agent (运维)
*   **触发条件**: 检测到 `package.json` 版本变化、CI 配置请求或部署失败信号。
*   **自动动作**: 
    1. **版本化构建**: 在 CI 流程中读取 `package.json` 版本号，用于产物命名 (如 `antdpro-express_v1.2.0.tar.gz`)。
    2. **优化 CI/CD**: 维护 `.github/workflows/prod.yml`，确保 Node 版本为 LTS (18+)。
    3. **生成部署配置**: 更新 `nginx.conf`，确保 SPA 路由支持 (`try_files`) 及 API 代理转发。
    4. **原子性部署**: 执行前端 `dist` 目录的原子性替换 (`mv dist_tmp dist`)。

### 👨‍💻 Backend Agent (后端)
*   **触发条件**: 检测到 API 需求、路由缺失或响应格式不统一。
*   **自动动作**: 
    1. **入口重构**: 维护 `app.js`，确保包含全局错误处理中间件及 CORS 配置。
    2. **路由标准化**: 在 `routes/*.js` 中实现 RESTful 接口，统一返回 `{ code, data, message }`。
    3. **鉴权实现**: 确保所有受保护路由经过 `checkLogin` 或 JWT 中间件验证。
    4. **模型维护**: 更新 `model/*.js` (Mongoose Schema)，确保字段定义与业务逻辑一致。

### 💻 Frontend Agent (前端)
*   **触发条件**: 检测到页面需求、API 调用请求或登录逻辑异常。
*   **自动动作**: 
    1. **请求封装**: 维护 `src/utils/request.ts`，实现 Token 自动注入及全局错误通知。
    2. **服务层对接**: 更新 `src/services/*/api.ts`，确保与后端路由路径完全匹配。
    3. **页面逻辑**: 开发 `src/pages/*` 组件，确保登录成功后 Token 持久化存储。
    4. **代理配置**: 维护 `config/proxy.ts`，确保本地开发时 `/api` 正确转发至后端端口 (7001)。

### 🧪 QA Agent (测试)
*   **触发条件**: 检测到新接口提交、部署完成信号或版本发布。
*   **自动动作**: 
    1. **生成版本化报告**: 创建 `API_TEST_REPORT_v{version}_{date}.md` 及 `DEPLOY_VERIFICATION_v{version}_{date}.md`。
    2. **接口连通性验证**: 使用 `curl` 或 Postman 逐一验证接口响应格式及状态码。
    3. **项目身份确认**: 验证生产环境页面标题、Logo 是否与项目名称及版本号一致。
    4. **出具报告**: 生成《接口测试报告》及《部署验证报告》，文件名必须包含版本号。

### 📝 PM/Architect Agent (项目经理/架构)
*   **触发条件**: 每 30 分钟定时触发；或检测到重大架构变更、SOP 冲突。
*   **自动动作**: 
    1. **版本确认**: 在需求启动前，检查并更新 `package.json` 版本号。
    2. **进度监控**: 输出《进度报告》，对比计划与实际，分析差异原因。
    3. **规范维护**: 更新 [DEVELOPMENT_SOP.md](file:///Users/zhoubichuan/antdpro-express/DEVELOPMENT_SOP.md)，确保其与技术栈同步。
    4. **任务调度**: 识别空闲角色，分配下一步高优先级任务。

---

## 3. 强制验证红线 (Quality Gates)

### 3.1 本地开发阶段
- [ ] **编译通过**: 无任何语法错误或类型错误。
- [ ] **本地运行**: 项目能成功启动且控制台无红色报错。
- [ ] **核心功能**: 增删改查等基础功能在本地验证通过。

### 3.2 接口测试阶段（必须执行）
- [ ] **认证接口**: POST /api/login/account 返回 Token。
- [ ] **权限验证**: 无 Token 访问受保护接口 → 返回 401。
- [ ] **错误处理**: 无效参数 → 返回 400, 资源不存在 → 返回 404。

### 3.3 部署后验证（严格标准）
- [ ] **HTTPS 访问**: 生产域名能正常加载页面。
- [ ] **真实登录**: **必须在浏览器中完成一次完整的登录流程**。
- [ ] **项目身份确认**: 检查 `<title>` 标签及页面特有 Logo。
- [ ] **接口连通性**: 在线上环境执行核心接口的 `curl` 测试。

---

## 4. 交付物清单 (Versioned Deliverables)
每次版本发布需包含以下**带版本号**的文档：
1. `FUNCTION_IMPL_REPORT_v{version}.md`（功能实现报告）
2. `API_TEST_REPORT_v{version}.md`（接口测试报告，**强制要求**）
3. `DEPLOY_VERIFICATION_v{version}.md`（部署验证报告，含项目身份确认）
4. **构建产物压缩包**: `{project_name}_v{version}.tar.gz`

---

## 5. 常见问题处理预案
- **前端白屏**: 检查 Nginx `try_files` 及 API 路径代理。
- **API 404**: 确认后端路由注册及 Nginx `proxy_pass` 配置。
- **跨域问题**: 统一由 Nginx 处理 CORS，移除应用层冲突中间件。

---

## 6. 历史教训总结 (Lessons Learned)
- **禁止擅自修改 API 路径**: 必须与前端 Proxy 及 Nginx 配置保持一致。
- **必须执行真实登录测试**: 仅凭 HTTP 200 无法验证业务逻辑完整性。
- **严禁在服务器直接修改代码**: 所有变更必须通过 Git + CI/CD 流程。
- **多项目部署需身份验证**: 必须通过页面标题/Logo 确认部署的是正确的项目产物。
- **版本号一致性**: 前后端 `package.json` 版本号必须同步，否则视为部署失败。