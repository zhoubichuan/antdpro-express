# 🚀 全栈自动化协作与部署流程规范 (SOP v6.4 - 报告规范化版)

## ⚠️ 核心原则（必须严格遵守）

1.  **版本号先行**: **每次需求开发前必须先确认并更新项目版本号**。所有分支、报告、交付物必须携带该版本号。
2.  **生产部署分支锁定**: **生产环境（Production）必须且只能从 `release/v{version}` 分支进行部署**。严禁直接使用 `dev` 或 `feature` 分支部署生产。
3.  **全量接口测试**: **《接口测试报告》必须覆盖当前版本涉及的所有后端接口**，不得仅选取部分核心接口。
4.  **报告文件化**: **所有过程报告及交付报告必须输出为 `.md` (Markdown) 格式文档**，并严格按照 `{REPORT_NAME}_v{version}.md` 命名。
5.  **空闲即执行 (Idle = Execute)**: 任务完成后立即领取下一项，严禁等待指令。
6.  **人类时间汇报**: 每 30 分钟输出进度报告，不阻塞任务流。

---

## 1. 版本管理与分支策略 (Version & Git Flow)

### 1.1 分支定义与职责
- **master**: 生产环境稳定分支，仅接受来自 `release` 的合并。
- **release/v{version}**: **发布分支**。用于集成测试、预发布及**生产环境部署**。
- **dev**: 日常开发分支，功能开发在此分支进行。
- **feature/{version}/{name}**: 特性分支。用于开发新功能，从 `dev` 拉取，合并回 `dev`。

### 1.2 强制流转规则
```
feature/v1.0.0/xxx -> dev (代码审查后合并)
dev -> release/v1.0.0 (功能冻结，准备发布)
release/v1.0.0 -> master (测试通过后合并并打 Tag: v1.0.0)
```
**注意**: 生产环境部署动作必须在 `release/v{version}` 分支上进行。

---

## 2. 角色自动执行清单 (Auto-Execution List) - **核心驱动**

### 👷‍♂️ DevOps Agent (运维)
*   **触发条件**: 检测到 `package.json` 版本变化、CI 配置请求或部署失败信号。
*   **自动动作**: 
    1. **分支检查**: 部署前校验当前分支是否为 `release/*`，若非此分支则终止部署并报警。
    2. **版本化构建**: 在 CI 流程中读取 `package.json` 版本号，用于产物命名 (如 `antdpro-express_v1.0.0.tar.gz`)。
    3. **原子性部署**: 执行前端 `dist` 目录的原子性替换 (`mv dist_tmp dist`)。
    4. **进程管理**: 使用 PM2 优雅重启后端服务 (`pm2 reload`)。

### 👨‍💻 Backend Agent (后端)
*   **触发条件**: 检测到 API 需求、路由缺失或响应格式不统一。
*   **自动动作**: 
    1. **入口重构**: 维护 `app.js`，确保包含全局错误处理中间件及 CORS 配置。
    2. **路由标准化**: 在 `routes/*.js` 中实现 RESTful 接口，统一返回 `{ code, data, message }`。
    3. **鉴权实现**: 确保所有受保护路由经过 `checkLogin` 或 JWT 中间件验证。

### 💻 Frontend Agent (前端)
*   **触发条件**: 检测到页面需求、API 调用请求或登录逻辑异常。
*   **自动动作**: 
    1. **请求封装**: 维护 `src/utils/request.ts`，实现 Token 自动注入及全局错误通知。
    2. **服务层对接**: 更新 `src/services/*/api.ts`，确保与后端路由路径完全匹配。

### 🧪 QA Agent (测试)
*   **触发条件**: 检测到新接口提交、部署完成信号或版本发布。
*   **自动动作**: 
    1. **全量接口扫描**: 遍历后端 `routes/` 目录，提取所有注册的 API 路径。
    2. **生成全量报告**: 创建 `API_TEST_REPORT_v{version}.md`，**必须包含所有接口的测试结果**。
    3. **连通性验证**: 逐一验证每个接口的响应格式、状态码及业务逻辑正确性。
    4. **项目身份确认**: 验证生产环境页面标题、Logo 是否与项目名称及版本号一致。

### 📝 PM/Architect Agent (项目经理/架构)
*   **触发条件**: 每 30 分钟定时触发；或检测到重大架构变更、SOP 冲突。
*   **自动动作**: 
    1. **版本确认**: 在需求启动前，检查并更新 `package.json` 版本号。
    2. **进度监控**: 输出《进度报告》，对比计划与实际，分析差异原因。
    3. **规范维护**: 更新 [DEVELOPMENT_SOP.md](file:///Users/zhoubichuan/antdpro-express/DEVELOPMENT_SOP.md)，确保其与技术栈同步。
    4. **任务调度**: 识别空闲角色，分配下一步高优先级任务。

---

## 3. 强制验证红线 (Quality Gates)

### 3.1 本地开发阶段
- [ ] **编译通过**: 无任何语法错误或类型错误。
- [ ] **本地运行**: 项目能成功启动且控制台无红色报错。

### 3.2 接口测试阶段（全量强制要求）
- [ ] **全量覆盖**: 测试报告必须列出项目中**每一个**已实现的接口。
- [ ] **认证接口**: POST /api/login/account 返回 Token。
- [ ] **权限验证**: 无 Token 访问受保护接口 → 返回 401。
- [ ] **错误处理**: 无效参数 → 返回 400, 资源不存在 → 返回 404。

### 3.3 部署后验证（严格标准）
- [ ] **分支合规**: 确认部署代码源自 `release/v{version}` 分支。
- [ ] **HTTPS 访问**: 生产域名能正常加载页面。
- [ ] **真实登录**: **必须在浏览器中完成一次完整的登录流程**。
- [ ] **项目身份确认**: 检查 `<title>` 标签及页面特有 Logo。

---

## 4. 交付物清单 (Versioned Deliverables)
每次版本发布需包含以下**带版本号的 Markdown 文档**：
1. **`FUNCTION_IMPL_REPORT_v{version}.md`**: 功能实现报告，记录开发细节与自测结果。
2. **`API_TEST_REPORT_v{version}.md`**: **全量接口测试报告**，强制要求覆盖所有 API。
3. **`DEPLOY_VERIFICATION_v{version}.md`**: 部署验证报告，含真实登录验证及项目身份确认。
4. **构建产物压缩包**: `{project_name}_v{version}.tar.gz`。

---

## 5. 历史教训总结 (Lessons Learned)
- **禁止擅自修改 API 路径**: 必须与前端 Proxy 及 Nginx 配置保持一致。
- **必须执行真实登录测试**: 仅凭 HTTP 200 无法验证业务逻辑完整性。
- **严禁在服务器直接修改代码**: 所有变更必须通过 Git + CI/CD 流程。
- **多项目部署需身份验证**: 必须通过页面标题/Logo 确认部署的是正确的项目产物。
- **部署分支锁定**: 生产环境严禁使用非 release 分支，防止未经验证的代码上线。
- **全量测试必要性**: 局部测试容易遗漏边缘接口，必须执行全量扫描测试。
- **报告文件化**: 所有测试与验证结果必须固化为 `.md` 文件并纳入版本管理，禁止口头汇报或临时文本。