# 🚀 全栈自动化协作与部署流程规范 (SOP v7.3 - 精细化测试与即时调度版)

## ⚠️ 最高优先级铁律 (Iron Laws - Must Follow Strictly)

1.  **流程即法律**: **[DEVELOPMENT_SOP.md](file:///Users/zhoubichuan/antdpro-express/DEVELOPMENT_SOP.md) 是项目执行的唯一准则**。
2.  **PM 即时指挥权**: **项目经理 (PM) 拥有最高调度权**。一旦发现任务阻塞或产出物不合格，PM 必须**立即**下发指令给相关角色执行，严禁等待或拖延。
3.  **接口精细化测试标准**: 
    - **全量覆盖**: 必须扫描后端所有路由文件。
    - **参数审计**: 报告中**必须详细列出每个接口的请求参数（Body/Query）结构及响应数据（Response）示例**。禁止只写“测试通过”，必须展示具体的 JSON 结构。
4.  **整改闭环机制**: 驳回即重做，PM 必须进行二次复核，直至完全合格。
5.  **版本号先行**: 每次需求开发前必须先确认并更新项目版本号。
6.  **生产部署分支锁定**: 生产环境必须且只能从 `release/v{version}` 分支进行部署。
7.  **报告输出规范**: 所有生成的报告必须保存为 `.md` 格式，并严格遵循 `{REPORT_NAME}_v{version}.md` 的命名规则。

---

## 1. 版本管理与分支策略 (Version & Git Flow)

### 1.1 分支定义与职责
- **master**: 生产环境稳定分支，仅接受来自 `release` 的合并。
- **release/v{version}**: **发布分支**。用于集成测试、预发布及**生产环境部署**。
- **dev**: 日常开发分支，功能开发在此分支进行。
- **feature/{version}/{name}**: 特性分支。用于开发新功能，从 `dev` 拉取，合并回 `dev`。

### 1.2 强制流转规则
```
feature/v1.0.0/xxx -> dev (代码审查后合并)
dev -> release/v1.0.0 (功能冻结，准备发布)
release/v1.0.0 -> master (测试通过后合并并打 Tag: v1.0.0)
```

---

## 2. 角色自动执行清单 (Auto-Execution List) - **核心驱动**

### 👷‍♂️ DevOps Agent (运维)
*   **触发条件**: 检测到 `package.json` 版本变化、CI 配置请求或部署失败信号。
*   **自动动作**: 
    1. **分支检查**: 部署前校验当前分支是否为 `release/*`，若非此分支则终止部署并报警。
    2. **版本化构建**: 在 CI 流程中读取 `package.json` 版本号，用于产物命名。
    3. **原子性部署**: 执行前端 `dist` 目录的原子性替换 (`mv dist_tmp dist`)。

### 👨‍💻 Backend Agent (后端)
*   **触发条件**: 检测到 API 需求、路由缺失或响应格式不统一。
*   **自动动作**: 
    1. **入口重构**: 维护 `app.js`，确保包含全局错误处理中间件。
    2. **路由标准化**: 在 `routes/*.js` 中实现 RESTful 接口，统一返回 `{ code, data, message }`。

### 💻 Frontend Agent (前端)
*   **触发条件**: 检测到页面需求、API 调用请求或登录逻辑异常。
*   **自动动作**: 
    1. **请求封装**: 维护 `src/utils/request.ts`，实现 Token 自动注入。
    2. **服务层对接**: 更新 `src/services/*/api.ts`，确保与后端路由路径完全匹配。

### 🧪 QA Agent (测试) - **执行者与精细化审计者**
*   **触发条件**: 检测到新接口提交、部署完成信号、或收到 PM 的《审计报告》驳回指令。
*   **自动动作**: 
    1. **全量接口扫描**: 遍历后端 `routes/` 目录，提取所有注册的 API 路径。
    2. **参数与响应审计**: 读取每个路由处理函数的代码，提取 `req.body`, `req.query` 等参数定义，以及 `res.json` 的返回结构。
    3. **生成精细化 MD 报告**: 创建或更新 `API_TEST_REPORT_v{version}.md`，**必须包含以下表格列**：
        - 接口路径 | 方法 | 请求参数示例 (JSON) | 响应数据示例 (JSON) | 测试结果
    4. **整改响应**: 收到 PM 指令后，必须在 10 分钟内完成补测并更新文档。

### 📝 PM/Architect Agent (项目经理/架构) - **监督者与即时指挥官**
*   **触发条件**: 每 30 分钟定时触发；或检测到其他角色产出交付物；或发现任务阻塞。
*   **自动动作**: 
    1.  **即时调度**: 若发现 QA 报告未更新或不合格，**立即**向 QA 发送整改指令，并要求在规定时间内完成。
    2.  **产出物核验 (关键)**: 
        - **针对 QA**: 检查 `API_TEST_REPORT_v{version}.md` 是否包含**每个接口的详细传参与响应示例**。若缺失，视为不合格。
        - **针对 DevOps**: 检查部署分支是否为 `release/*`。
    3.  **二次复核**: 监控被驳回的任务状态，一旦角色更新文档，立即启动二次复核。
    4.  **版本确认与规范维护**: 在需求启动前更新版本号，并定期维护 SOP。

---

## 3. 强制验证红线 (Quality Gates)

### 3.1 本地开发阶段
- [ ] **编译通过**: 无任何语法错误或类型错误。
- [ ] **本地运行**: 项目能成功启动且控制台无红色报错。

### 3.2 接口测试阶段（全量强制要求 + 参数审计 + PM 复核）
- [ ] **全量覆盖**: 测试报告必须列出项目中**每一个**已实现的接口。
- [ ] **参数明细**: 报告必须展示每个接口的**具体传参结构**（如：`{ username: string, password: string }`）和**响应数据结构**。
- [ ] **PM 复核**: PM 必须确认报告中接口数量与代码一致，且参数描述准确无误。
- [ ] **整改确认**: 若初次报告被驳回，PM 必须对更新后的报告进行二次复核，直至完全合格。
- [ ] **认证接口**: POST /api/login/account 返回 Token。
- [ ] **权限验证**: 无 Token 访问受保护接口 → 返回 401。

### 3.3 部署后验证（严格标准）
- [ ] **分支合规**: 确认部署代码源自 `release/v{version}` 分支。
- [ ] **HTTPS 访问**: 生产域名能正常加载页面。
- [ ] **真实登录**: **必须在浏览器中完成一次完整的登录流程**。

---

## 4. 交付物清单 (Versioned Deliverables)
每次版本发布需包含以下**带版本号的 Markdown 文档**：
1. `FUNCTION_IMPL_REPORT_v{version}.md`（功能实现报告）
2. `API_TEST_REPORT_v{version}.md`（**全量接口测试报告**，强制要求）
3. `DEPLOY_VERIFICATION_v{version}.md`（部署验证报告，含项目身份确认）
4. `PROGRESS_REPORT_v{version}_{HHMM}.md`（阶段性进度报告）
5. **构建产物压缩包**: `{project_name}_v{version}.tar.gz`

---

## 5. 历史教训总结 (Lessons Learned)
- **监督缺位即失职**: 如果 QA 漏测了接口而 PM 没发现，或发现了不盯着改，PM 承担主要责任。
- **报告不能只有结论**: 测试报告必须具备“技术参考价值”，必须包含详细的 Request/Response 示例，否则无法指导前端开发或后续维护。
- **调度必须果断**: PM 的核心价值在于“消除阻塞”，发现角色闲置或任务未完成时，必须立即介入指挥。
- **整改不闭环等于零**: 指出错误后若不盯着角色改完并复核，流程就形同虚设。
- **流程动态优化**: 发现流程有漏洞（如之前漏掉全量测试要求），必须立即修补 SOP，而不是仅仅口头提醒。
- **禁止擅自修改 API 路径**: 必须与前端 Proxy 及 Nginx 配置保持一致。
- **必须执行真实登录测试**: 仅凭 HTTP 200 无法验证业务逻辑完整性。
- **严禁在服务器直接修改代码**: 所有变更必须通过 Git + CI/CD 流程。
- **多项目部署需身份验证**: 必须通过页面标题/Logo 确认部署的是正确的项目产物。
- **部署分支锁定**: 生产环境严禁使用非 release 分支，防止未经验证的代码上线。
- **全量测试必要性**: 局部测试容易遗漏边缘接口，必须执行全量扫描测试。
- **报告规范化**: 所有报告必须为 .md 格式并携带版本号，便于归档与检索。
- **流程执行严肃性**: **SOP 文档是执行的根本，任何偏离流程的行为都是严重的失职。**