# 🚀 全栈自动化协作与部署流程规范 (SOP v7.5 - 真实环境核验版)

## ⚠️ 最高优先级铁律 (Iron Laws - Must Follow Strictly)

1.  **流程即法律**: **[DEVELOPMENT_SOP.md](file:///Users/zhoubichuan/antdpro-express/DEVELOPMENT_SOP.md) 是项目执行的唯一准则**。
2.  **PM 线上真实性探测**: **项目经理 (PM) 必须在验收任何报告前，亲自通过 curl 或浏览器访问生产环境域名**。若返回 404、502 或白屏，立即判定部署失败，驳回所有“测试通过”的报告。
3.  **即时指挥权**: 发现任务阻塞或产出物不合格，必须**立即**下发指令。
4.  **端到端验收标准**: 
    - **前端可用性**: 生产环境域名必须能在浏览器中正常加载页面，无白屏、无 404。
    - **功能交互**: 必须在浏览器中完成核心业务流程（如：登录 -> 跳转 -> 数据展示）。
5.  **整改闭环机制**: 驳回即重做，PM 必须进行二次复核，直至完全合格。
6.  **版本号先行**: 每次需求开发前必须先确认并更新项目版本号。
7.  **生产部署分支锁定**: 生产环境必须且只能从 `release/v{version}` 分支进行部署。
8.  **报告输出规范**: 所有生成的报告必须保存为 `.md` 格式，并严格遵循 `{REPORT_NAME}_v{version}.md` 的命名规则。

---

## 2. 角色自动执行清单 (Auto-Execution List) - **核心驱动**

### 📝 PM/Architect Agent (项目经理/架构) - **监督者与真实性核验者**
*   **触发条件**: 每 30 分钟定时触发；或检测到其他角色产出交付物。
*   **自动动作**: 
    1.  **线上连通性探测 (关键)**: 在 QA 提交报告后，PM 必须执行 `curl -I <prod_url>`。**若状态码非 200，视为严重事故，立即启动故障排查流程。**
    2.  **即时调度**: 若发现 QA 报告未更新或不合格，**立即**向 QA 发送整改指令。
    3.  **产出物核验**: 检查 `API_TEST_REPORT_v{version}.md` 及 `FRONTEND_FUNCTIONAL_REPORT_v{version}.md` 的真实性。
    4.  **二次复核**: 监控被驳回的任务状态，一旦角色更新文档，立即启动二次复核。

### 👷‍♂️ DevOps Agent (运维) - **部署执行者**
*   **触发条件**: 检测到发布分支合并或部署指令。
*   **自动动作**: 
    1.  **Nginx 配置校验**: 确保 `nginx.conf` 中 `location /antdpro-demo` 路径正确指向了前端 `dist` 目录。
    2.  **部署日志审计**: 检查 SSH 部署脚本的执行日志，确认文件已上传且权限正确。

---

## 3. 强制验证红线 (Quality Gates)

### 3.3 部署后验证（严格标准 + 真实探测）
- [ ] **HTTP 状态码**: PM 必须确认生产域名返回 **200 OK**。
- [ ] **HTTPS 访问**: 生产域名能正常加载页面，无白屏。
- [ ] **真实登录**: **必须在浏览器中完成一次完整的登录流程**。
- [ ] **项目身份确认**: 检查 `<title>` 标签及页面特有 Logo。
- [ ] **前端报告**: 必须产出 `FRONTEND_FUNCTIONAL_REPORT_v{version}.md`，且内容与 PM 探测结果一致。

---

## 5. 历史教训总结 (Lessons Learned)
- **真实性高于一切**: 绝不能仅凭 AI 生成的文字就相信系统已上线，必须通过真实的网络请求（curl/浏览器）来验证。
- **404 即事故**: 生产环境返回 404 说明 Nginx 配置或部署路径存在严重错误，必须立即修复。
- **监督缺位即失职**: 如果 QA 报告说“通过”而 PM 没去核实，PM 承担主要责任。
- **报告不能只有结论**: 测试报告必须具备“技术参考价值”，必须包含详细的 Request/Response 示例，否则无法指导前端开发或后续维护。
- **调度必须果断**: PM 的核心价值在于“消除阻塞”，发现角色闲置或任务未完成时，必须立即介入指挥。
- **整改不闭环等于零**: 指出错误后若不盯着角色改完并复核，流程就形同虚设。
- **流程动态优化**: 发现流程有漏洞（如之前漏掉全量测试要求），必须立即修补 SOP，而不是仅仅口头提醒。
- **禁止擅自修改 API 路径**: 必须与前端 Proxy 及 Nginx 配置保持一致。
- **必须执行真实登录测试**: 仅凭 HTTP 200 无法验证业务逻辑完整性。
- **严禁在服务器直接修改代码**: 所有变更必须通过 Git + CI/CD 流程。
- **多项目部署需身份验证**: 必须通过页面标题/Logo 确认部署的是正确的项目产物。
- **部署分支锁定**: 生产环境严禁使用非 release 分支，防止未经验证的代码上线。
- **全量测试必要性**: 局部测试容易遗漏边缘接口，必须执行全量扫描测试。
- **报告规范化**: 所有报告必须为 .md 格式并携带版本号，便于归档与检索。
- **流程执行严肃性**: **SOP 文档是执行的根本，任何偏离流程的行为都是严重的失职。**
- **前端不可忽略**: 接口通了不代表项目能用，必须确保浏览器端页面能正常打开并完成业务交互。