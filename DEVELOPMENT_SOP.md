# 全栈自动化协作与部署流程规范（通用版）

## 一、最高优先级铁律

1. **流程即法律**：本规范是项目执行的唯一准则，所有开发、测试、部署动作必须严格遵循。
2. **零重复犯错原则**：
   - **增量融合**：任何规范修改必须是增量式的，严禁通过“重写”覆盖核心流程（如分支管理、CI/CD、部署架构），需确保历史核心规范完整保留。
   - **全量审计**：项目经理在每次提交规范变更前，必须对照“核心管控模块清单”（见附录）逐项核对，缺失一项即为严重违规。
3. **动态二级路由部署强制约束**：
   - **核心原则**：前后端统一通过以项目名称命名的二级路径部署，路径前缀从 `package.json` 的 `name` 字段或工作区目录名自动提取。
   - **严禁硬编码**：配置文件、脚本中不得写死具体路径字符串，需通过变量或工具函数动态获取。
4. **项目经理即时指挥权与真实性探测**：
   - **即时调度**：发现任务阻塞、报告不合格或流程偏离，必须立即下发指令强制整改，严禁等待。
   - **线上核验**：验收前必须亲自执行 `curl -I <prod_url>`，若状态码非 200，视为严重事故，立即驳回所有报告。
5. **整改闭环机制**：驳回即重做，项目经理必须进行二次复核，直至完全合格。
6. **版本号先行**：每次需求开发前必须先确认并更新项目版本号（`package.json` 的 `version` 字段）。
7. **生产部署分支锁定**：生产环境必须且只能从 `release/v{version}` 分支进行部署。
8. **报告输出规范**：所有生成的报告必须保存为 `.md` 格式，并严格遵循 `{报告名称}_v{version}.md` 的命名规则（如 `前端开发总结报告_v1.0.0.md`）。

---

## 二、技术栈与架构规范

1. **技术选型（通用框架，可按项目调整）**：
   - **前端**：React/Vue/Angular 等主流框架（如 Ant Design Pro、Next.js、Nuxt.js），需支持 TypeScript。
   - **后端**：Node.js/Python/Java 等主流语言（如 Express、Flask、Spring Boot），需支持 RESTful API。
   - **数据库**：关系型（MySQL/PostgreSQL）或非关系型（MongoDB/Redis），需通过 ORM/ODM 管理。
   - **包管理**：npm/yarn/pip/maven 等，需统一团队工具。
   - **进程管理**：PM2/Systemd/Docker 等，需支持优雅重启与日志管理。
2. **部署架构**：
   - **模式**：动态二级路径部署（基于 `package.json` 的 `name`）。
   - **反向代理（以 Nginx 为例）**：
     - **静态资源**：`location /${project_name}/` 指向 `dist/` 目录，开启 Gzip 压缩。
     - **API 转发**：`location /${project_name}/api/` 转发至后端服务端口，设置 `proxy_pass` 与必要 Header（如 `X-Real-IP`）。
     - **SPA 支持**：根路径配置 `try_files $uri $uri/ /index.html`，避免刷新 404。

---

## 三、版本控制与分支管理

1. **分支策略**：
   - `master`：生产环境稳定分支，仅接受来自 `release` 的合并。
   - `dev`：日常开发分支，集成所有已完成的 feature。
   - `feature/{version}/{name}`：特性分支（如 `feature/v1.0.0/user-login`），从 `dev` 切出，开发完成后合并回 `dev`。
   - `release/v{version}`：发布分支，从 `dev` 切出，用于生产环境部署前的最终测试与修复，修复后合并回 `dev` 和 `master`。
2. **版本化管理规范**：
   - **版本号确认**：每次需求开发前必须先更新 `package.json` 的 `version` 字段（遵循语义化版本规范）。
   - **交付物规范**：所有测试报告、验证报告文件名必须包含版本号标识（如 `接口测试报告_v1.0.0.md`）。

---

## 四、CI/CD 自动化流程

1. **触发源**：Git 平台（如 Gitee/GitHub/GitLab）的 Actions 或 Webhook，支持分支/标签触发。
2. **前端构建**：
   - 执行 `npm run build` 生成 `dist/` 目录。
   - 通过 SSH/SCP 原子性替换服务器静态资源，保留旧版本备份（便于回滚）。
3. **后端部署**：
   - 上传源码至服务器，执行 `npm install --production`（或对应语言的依赖安装命令）。
   - 使用进程管理工具（如 PM2）优雅重启服务，避免请求中断。
4. **环境变量**：敏感信息（如数据库密码、JWT 密钥）通过平台 Secrets 管理，`.env` 文件需被 `.gitignore` 忽略，生产环境通过配置文件或环境变量注入。

---

## 五、API 设计与交互规范

1. **路径规范**：
   - **统一前缀**：前端请求后端 API 时，路径前缀格式统一为 `/${backend_service_name}/api`（`backend_service_name` 与 `package.json` 的 `name` 一致）。
   - **示例**：若后端服务名为 `my-fullstack-app`，则接口路径为 `/my-fullstack-app/api/users`。
2. **响应格式**：
   - **统一结构**：`{ code: Number, data: Any, message: String }`，`code=200` 表示成功，非 200 表示错误（如 400 参数错误、500 服务器错误）。
   - **错误处理**：后端必须配置全局错误处理中间件，捕获未处理的异常并返回统一格式；前端通过请求库（如 umi-request、axios）拦截器统一处理 HTTP 错误通知（如弹窗提示）。

---

## 六、项目经理核心管控流程

1. **开发计划制定**：
   - **触发条件**：需求评审通过后，版本开发启动前。
   - **核心动作**：
     - 拆解需求为具体开发任务，明确每个任务的负责人、计划起止时间、依赖项及交付物（需与“角色自动执行清单”中的交付物对应）。
     - 生成《开发计划表_v{version}.md》，作为后续进度跟踪的基准。
2. **任务派发与监督**：
   - **派发指令**：根据《开发计划表》的时间节点，向对应角色下发正式任务指令，明确任务要求、交付物及截止时间。
   - **过程监督**：实时跟踪任务进度，发现阻塞（如依赖任务未完成）或偏离（如进度滞后）时，立即介入协调或调整计划。
3. **交付物核实与任务闭环**：
   - **核实标准**：角色提交的报告需内容真实、完整，符合规范要求（如《后端接口变更说明》需与代码一致，《自测用例清单》需覆盖核心功能）。
   - **闭环动作**：核实通过后，正式关闭任务；若驳回，需明确整改要求并跟踪二次提交，直至合格。
4. **定时进度报告**：
   - **频率**：每隔 30 分钟输出一份《进度报告_v{version}.md》。
   - **核心内容**：
     - 对比《开发计划表》，列出每个任务的“计划状态”与“实际状态”。
     - 分析进度偏差（如提前/滞后原因），识别潜在风险（如依赖任务未完成导致后续阻塞）。
     - 同步当前已完成的任务、进行中的任务及下一步计划。

---

## 七、角色自动执行清单

1. **前端开发**：
   - **触发条件**：需求确认、接口定义完成或 Bug 修复后，或收到项目经理派发任务的指令。
   - **自动动作**：
     - **组件开发与自测**：遵循团队 UI 规范开发页面与组件，确保本地 `npm start` 运行无报错。
     - **接口联调**：配合后端完成 API 连通性测试，处理 Token 注入及全局错误拦截。
   - **产出物交付**：
     - **《前端开发总结报告》**：记录新增/修改的页面路由、核心组件逻辑及 UI 交互细节。
     - **《自测用例清单》**：列出已完成的功能点自测结果（如登录、列表加载、表单提交）。
     - **构建产物**：确保 `npm run build` 生成的 `dist/` 目录结构符合二级路由部署规范（如静态资源路径含 `/${project_name}/`）。
2. **后端开发**：
   - **触发条件**：数据库模型设计完成或业务逻辑实现后，或收到项目经理派发任务的指令。
   - **自动动作**：
     - **接口实现与单元测试**：编写路由及 Controller 逻辑，确保核心逻辑有对应的单元测试覆盖（如 Jest、Mocha）。
     - **API 文档同步**：维护路由文件中的注释（如 Swagger/OpenAPI 格式），确保参数定义与实际代码一致。
   - **产出物交付**：
     - **《后端接口变更说明》**：详细列出新增/修改的 API 路径、请求方法、入参格式及响应示例。
     - **《数据库迁移脚本》**：若有 Schema 变动，需提供对应的迁移命令或脚本（如 SQL 文件、Mongoose 迁移脚本）。
     - **《服务健康检查报告》**：记录本地启动日志、端口占用情况及依赖包安装状态。
3. **测试**：
   - **触发条件**：接收到前后端交付物或部署完成信号，或收到项目经理派发任务的指令。
   - **自动动作**：
     - **全量接口扫描**：遍历后端路由目录，提取所有 API。
     - **参数明细审计**：报告中必须展示每个接口的具体传参结构和响应数据结构。
     - **前端功能测试**：访问生产环境域名，完成真实登录及业务操作（如创建数据、修改配置）。
   - **产出物交付**：
     - **《接口测试报告》**：包含详细 Request/Response 示例的全量接口测试报告。
     - **《前端功能测试报告》**：记录浏览器端操作流程及截图的报告。
4. **运维**：
   - **触发条件**：收到部署指令或配置更新，或收到项目经理派发任务的指令。
   - **自动动作**：
     - **动态路径配置**：确保反向代理（如 Nginx）的 `location /${project_name}/` 正确指向静态资源目录。
     - **API 代理重写**：配置 `rewrite ^/${project_name}/api/(.*)$ /$1 break;`，将前端请求的二级路径转发至后端根路径。
   - **产出物交付**：
     - **《部署执行日志》**：记录 SSH 连接、文件上传、依赖安装及进程管理工具重启的详细过程。
     - **《环境配置清单》**：记录当前服务器的语言版本、反向代理配置路径及环境变量设置。
5. **项目经理/架构**：
   - **核心职责**：计划制定者、任务派发者、监督者、规范校准者、真实性核验者。
   - **自动动作**：
     - 制定《开发计划表》，明确任务分工与时间节点。
     - 派发任务指令，跟踪进度并协调资源。
     - 核实角色交付物，确保内容真实、完整。
     - 每隔 30 分钟输出《进度报告》，对比计划与实际进度，识别偏差与风险。
     - 规范同步：分配任务时必须声明动态二级路由规范。
     - 配置审计：检查反向代理配置、前端 `publicPath` 是否使用了动态变量（如通过 `process.env.PROJECT_NAME` 获取）。
     - 线上连通性探测：执行 `curl -I https://domain.com/${project_name}`，确认返回 200 OK。
     - 产出物复核：对前后端及测试提交的报告进行二次核对，确保内容真实、完整。
     - SOP 完整性审计：每次更新规范前，必须核对分支管理、CI/CD、交付物清单等核心模块是否缺失。

---

## 八、强制验证红线

1. **架构与配置阶段**：
   - **动态路径一致性**：前端 `publicPath`、反向代理 `location`、后端路由前缀必须完全匹配 `package.json` 中的项目名称。
   - **API 转发逻辑**：反向代理必须能将 `/${project_name}/api/xxx` 正确转发至后端服务的 `/api/xxx`。
2. **开发与联调阶段**：
   - **前端自测**：前端必须提供《自测用例清单》，证明核心交互逻辑已通过本地验证。
   - **后端文档**：后端必须提供《后端接口变更说明》，且内容与路由文件实际代码一致。
3. **接口测试阶段**：
   - **全量覆盖**：测试报告必须列出项目中每一个已实现的接口。
   - **参数明细**：报告必须展示每个接口的具体传参结构和响应数据结构。
4. **部署后验证**：
   - **HTTP 状态码**：项目经理必须确认 `https://domain.com/${project_name}` 返回 200 OK。
   - **真实登录**：必须在浏览器中完成一次完整的登录流程（如输入账号密码、跳转首页）。
   - **项目身份确认**：检查 `<title>` 标签及页面特有 Logo，确保部署的是目标项目。

---

## 九、历史教训总结

1. **拒绝硬编码**：任何涉及项目标识的配置都必须动态获取，严禁写死（如 `/my-app/` 应改为 `/${project_name}/`）。
2. **文档完整性**：优化规范时严禁误删核心管控流程（如分支管理、CI/CD），应采用增量融合方式（如新增章节而非覆盖原有内容）。
3. **真实性高于一切**：绝不能仅凭 AI 生成的文字就相信系统已上线，必须通过真实的网络请求（如 curl、浏览器访问）来验证。
4. **监督缺位即失职**：如果测试报告说“通过”而项目经理没去核实，项目经理承担主要责任。
5. **零重复犯错**：同样的错误（如漏掉前后端交付物、硬编码路径）绝不允许出现第二次，需在规范中明确记录并增加对应验证红线。

---

## 附录：核心管控模块清单

- 分支管理（Git Flow）
- CI/CD 自动化流程（构建、部署、环境变量）
- 动态二级路由部署规范
- 项目经理核心管控流程（计划制定、任务派发、交付物核实、进度报告）
- 角色交付物清单（前端、后端、测试、运维、项目经理）
- 强制验证红线（架构、开发、测试、部署）