# 🚀 全栈自动化协作与部署流程规范 (SOP v6.0 - 融合优化版)

## ⚠️ 核心原则（必须严格遵守）

### 1. 需求计划不可擅自修改
- **任何 API 路径、功能需求、技术选型必须以原始项目计划为准**
- **如需变更，必须先与用户确认并获得书面批准**
- **严禁 AI 自行决定修改需求计划中的任何细节**

### 2. API 路径规范 (适配当前项目)
- **格式**: `/api/{resource}`
- **说明**: 当前为单项目部署，统一使用 `/api` 前缀。若未来有多项目共存，需按 `{project_name}/api` 隔离。
- **前端配置**: 
  - 开发环境: `config/proxy.ts` 转发到 `http://localhost:7001`
  - 生产环境: Nginx `location /api/` 转发到后端服务

### 3. 空闲即执行 (Idle = Execute)
- 任何 AI 角色在完成当前任务后，**必须立即**从任务看板领取下一个任务并执行。
- **严禁**等待用户指令或询问“是否继续”。
- **Git 兜底**: 允许直接修改代码文件，错误通过 `git revert` 修复，无需事前请示。

---

## 1. 分支管理策略 (Git Flow)

### 1.1 分支定义
- **master**: 生产环境稳定分支，仅接受来自 `release` 的合并。
- **release/v{version}**: 发布分支。所有新功能开发完成后，必须合并至此分支进行集成测试和部署。
- **dev**: 日常开发分支，功能开发在此分支进行。
- **feature/{name}**: 特性分支，从 `dev` 拉取，开发完成后合并回 `dev`。

### 1.2 分支流转规则
```
feature -> dev (代码审查后合并)
dev -> release/v{version} (功能冻结，准备发布)
release/v{version} -> master (测试通过后合并并打 Tag)
```

---

## 2. 角色职责与工作流程

### 2.1 开发流程（严格执行）
1. **需求确认**: 仔细阅读原始项目计划文档。
2. **本地开发**: 在 `dev` 或 `feature` 分支完成代码编写。
3. **本地自测**: 
   - 执行 `npm run dev` 确保本地运行无报错。
   - **本地 curl 测试核心接口连通性**。
4. **提交代码**: `git add . && git commit -m "feat: description"`
5. **本地构建验证**: 
   - 前端: `npm run build`
   - 验证构建产物中的关键配置（如 API 路径）。
6. **出具《功能实现报告》**: 包含修改点、自测结果、影响范围。

### 2.2 测试 (QA) 流程（强制接口测试）
1. **接口测试（强制要求）**: 
   - **必须对所有后端接口进行完整测试**
   - **生成《接口测试报告》，包含每个接口的请求参数、响应内容、测试结果**
2. **页面联调**: **必须在真实浏览器中模拟用户操作**（如登录、增删改查）。
3. **内容正确性验证**: 
   - **必须通过页面标题、Logo、特定文本等唯一标识确认访问的是正确的项目**
   - **禁止仅凭 HTTP 200 状态码判断部署成功**

### 2.3 运维 (DevOps) 部署流程
1. **获取代码**: 从 `master` 分支触发 CI/CD。
2. **服务部署**: 
   - 前端: 原子性替换 `dist/` 目录。
   - 后端: `npm install --production` 并 `pm2 restart`。
3. **在线验证**: 
   - **必须通过 HTTPS 域名进行真实登录测试**
   - **必须执行核心接口的线上连通性测试**
   - 检查 Nginx 和后端日志，确认无 404/500 错误

---

## 3. 强制验证红线 (Quality Gates)

### 3.1 需求确认阶段
- [ ] **原始计划查阅**: 已阅读所有相关项目计划文档
- [ ] **需求无歧义**: 所有技术细节（API 路径、版本号、配置项）已与用户确认

### 3.2 本地开发阶段
- [ ] **编译通过**: 无任何语法错误或类型错误。
- [ ] **本地运行**: 项目能成功启动且控制台无红色报错。
- [ ] **核心功能**: 增删改查等基础功能在本地验证通过。

### 3.3 接口测试阶段（必须执行）
- [ ] **认证接口测试**: POST /api/login/account, GET /api/currentUser
- [ ] **业务接口测试**: 覆盖所有 CRUD 接口
- [ ] **权限验证测试**: 无 Token 访问受保护接口 → 返回 401
- [ ] **错误处理测试**: 无效参数 → 返回 400, 资源不存在 → 返回 404

### 3.4 部署后验证（严格标准）
- [ ] **HTTPS 访问**: 生产域名能正常加载页面（HTTP 200）。
- [ ] **API 路径匹配**: 浏览器 Network 面板显示的请求路径与需求计划一致。
- [ ] **真实登录**: **必须在浏览器中完成一次完整的登录流程**。
- [ ] **项目身份确认**: 
  - **必须通过页面标题验证**: 检查 `<title>` 标签内容是否与项目名称一致
  - **必须通过页面内容验证**: 检查页面中是否有项目特有的 Logo、文本、功能模块
- [ ] **接口连通性验证**: **必须在线上环境执行核心接口的 curl 测试**

---

## 4. 常见问题处理预案

### 4.1 前端页面白屏或无法登录
- **排查步骤**: 
  1. 检查浏览器控制台 Network 面板，确认 API 请求路径是否与计划一致。
  2. 确认 Nginx `location` 转发规则是否与前端请求路径匹配。

### 4.2 API 返回 404 Not Found
- **排查步骤**:
  1. 确认后端路由是否按需求注册了正确的路径前缀。
  2. 确认 Nginx `proxy_pass` 是否正确转发了请求。

### 4.3 跨域问题 (CORS)
- **解决方案**: 统一由 Nginx 添加 `Access-Control-Allow-Origin` 响应头，移除应用层 CORS 中间件以避免冲突。

---

## 5. 交付物清单
每次版本发布需包含以下文档：
1. 《功能实现报告》（开发侧，含需求计划对比、本地构建验证截图）
2. 《接口测试报告》（测试侧，**强制要求**，含所有接口的测试结果）
3. 《部署验证报告》（运维侧，含真实登录验证、**项目内容正确性验证**、**接口连通性测试**）
4. **构建产物压缩包**（命名格式：`{project}_v{version}.tar.gz`）

---

## 6. 进度汇报规范
- **频率**: 每 30 分钟（基于人类时钟）输出一份进度报告。
- **内容**: 必须包含计划时间与实际完成时间的对比，明确体现差异及阻塞点。
- **形式**: 不阻塞任务流，支持并行流式处理。